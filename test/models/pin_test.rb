require 'test_helper'

class PinTest < ActiveSupport::TestCase
  # test 'pin should not save without a title' do
  #   pin = Pin.new
  #   assert_not pin.save, 'Pin saved without a title is: TRUTHY, should be: FALSY'
  # end
end
